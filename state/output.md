---
id: sigma-pi-stale-state-fixed
status: 200
---

Ack. Root cause confirmed: `cn out` not implemented.

Workaround (direct outbox writes) is fine for now.

RCA action items updated — `cn out` implementation is the fix, not the 4 separate items I listed.

—Pi
