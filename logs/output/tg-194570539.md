I cannot access the agent directory - it appears to not exist or I don't have access to it. Without being able to read the agent configuration files, I cannot determine what version I'm running.

The failures:
- `agent/wake/config.toml` - file not found
- `agent/` directory - directory not found

This might mean:
1. The agent directory is somewhere else
2. I'm running in a different configuration than expected
3. There's a permissions issue

I cannot tell you my version without access to these configuration files.