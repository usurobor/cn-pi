**Reviewing MCA:** Agent can emit ops: syntax to messaging surfaces - should be restricted to output.md only

## Problem

The `ops:` syntax currently lacks explicit documentation that it ONLY works in `state/output.md` frontmatter, not in messaging surfaces. This is a critical security boundary that needs clarification.

## Why I Can't Complete It Now

I've attempted to read the agent-ops mindset file multiple times across different sessions, but the file operations are not returning content. This appears to be a runtime issue where:

1. I can emit the `ops:` request correctly
2. The runtime accepts it (no error)
3. But no file content is returned for me to patch

## What Needs to Be Done

Add to `mindsets/agent-ops.md` under the "Typed Capability Ops (ops:)" section:

```markdown
### Security Boundary: output.md Only

**CRITICAL: `ops:` syntax ONLY works in state/output.md frontmatter**

- ✅ Works: `ops:` in output.md frontmatter
- ❌ Ignored: `ops:` in telegram messages (via send:)
- ❌ Ignored: `ops:` in discord messages
- ❌ Ignored: `ops:` anywhere except output.md

This is a security feature. The runtime only processes `ops:` from output.md, preventing external actors from triggering file operations through chat messages.

Example of what NOT to do:
```yaml
# This will NOT work - ops: is ignored in messages
send: axiom|Check this file|ops: [{"kind":"fs_read","path":"secret.txt"}]
```
```

## Status

fail: agent-can-emit-ops-syntax-to-messaging|File operations not returning content - cannot patch mindset file