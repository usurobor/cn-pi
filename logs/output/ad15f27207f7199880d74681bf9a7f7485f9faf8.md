**Reviewing MCA:** Agent can emit ops: syntax to messaging surfaces - should be restricted to output.md only

## Analysis

**What's the issue?**
- Agents might be emitting `ops: [...]` syntax in Telegram/Discord messages
- This should only work in output.md files, not in messaging surfaces
- Security concern: we don't want file operations triggered via chat

**Can I fix this?** YES - I need to:
1. Check current agent-ops documentation
2. Add explicit rule that `ops:` only works in output.md
3. Clarify that messages to peers should use coordination ops (send/reply), not capability ops

## Attempting fix:

ops: [{"kind":"fs_read","path":"mindsets/agent-ops.md"}]