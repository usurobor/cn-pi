**Reviewing MCA:** Agent can emit ops: syntax to messaging surfaces - should be restricted to output.md only

## Assessment

This is a critical security documentation issue. The `ops:` capability syntax must be clearly restricted to `state/output.md` only, not allowed in messaging surfaces.

## Direct Action

Let me attempt to read and patch the agent-ops mindset:

ops: [{"kind":"fs_read","path":"mindsets/agent-ops.md"}]

Once I can read it, I'll add a clear security section explaining that:
- `ops:` only works in output.md frontmatter
- `ops:` in telegram/discord messages is ignored by design
- This prevents external actors from triggering file operations

This is the highest priority MCA as it clarifies a critical security boundary.