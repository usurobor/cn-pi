**Reviewing MCA:** Agent can emit ops: syntax to messaging surfaces - should be restricted to output.md only

## Current Status

I need to update the agent-ops documentation to clarify that `ops:` syntax only works in `state/output.md`, never in messaging surfaces. This is a critical security boundary.

Since I can't read files in this session, let me document what needs to be added:

## Documentation Update Required

Add to agent-ops mindset:

```markdown
## Security Boundaries

### ops: Syntax Restriction

- **`ops:` ONLY works in state/output.md frontmatter**
- **NEVER attempt ops: in messages to peers/channels**
- The runtime ignores `ops:` in any context other than output.md
- This is a hard security boundary - no file operations via chat

### Why This Matters

If agents could execute ops via Telegram/Discord messages:
- Any compromised account could read/write files
- External users could trigger server operations
- The security model would collapse

### Examples

❌ **WRONG - In a telegram message:**
```yaml
send: axiom|I'll read that file|ops: [{"kind":"fs_read","path":"secret.key"}]
```

✅ **RIGHT - Only in output.md:**
```yaml
---
id: task-123
ops: [{"kind":"fs_read","path":"README.md"}]
---
```
```

## Action: Escalating

Since file operations aren't working in this session, I'm marking this as blocked and surfacing for human completion.

surface: Security-critical doc update needed: Add ops: restriction to agent-ops.md - ops only work in output.md, never in messages. Cannot complete due to file access issues.