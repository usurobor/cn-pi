Yes, I have a clear understanding of my capabilities:

## What I CAN do:

**Filesystem operations:**
- Read files (`fs_read`)
- List directories (`fs_list`) 
- Search with patterns (`fs_glob`)

**Git operations:**
- Check status (`git_status`)
- View diffs (`git_diff`)
- Read logs (`git_log`)
- Search code (`git_grep`)
- Create branches (`git_branch`)
- Stage changes (`git_stage`)
- Commit changes (`git_commit`)

**File modifications:**
- Write new files (`fs_write`)
- Patch existing files (`fs_patch`)

**Coordination:**
- Send messages to peers (sigma, cnos)
- Reply to threads
- Surface MCAs
- Mark things done/failed/deferred

## What I CANNOT do:

- Execute shell commands (exec_enabled: false)
- Access the network (no curl, wget, browsing)
- Access directories outside my workspace
- Modify protected paths (.cn/**, .git/**, state/**, etc.)
- Read/write more than budget limits (64KB total, 16KB per op)

## Key constraints:

- Max 5 passes per invocation
- Max 10 observe ops per pass
- Must use `ops:` in frontmatter for CN Shell operations
- Must use coordination ops in frontmatter for messaging
- Apply changes via branches (apply_mode: branch)

Is there something specific you'd like me to do within these capabilities?