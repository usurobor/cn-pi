**Reviewing MCA:** Agent can emit ops: syntax to messaging surfaces - should be restricted to output.md only

## Status: Blocked

I've attempted to read the agent-ops documentation multiple times but am not receiving file contents. Without read access, I cannot:

1. Verify the current documentation state
2. Create an appropriate patch
3. Complete this security-critical fix

## The Fix Needed

Add to agent-ops/SKILL.md:

```markdown
### Security Boundary

- `ops:` syntax ONLY works in `state/output.md` frontmatter
- NEVER attempt `ops:` in messages sent to peers/channels
- Messages containing `ops:` arrays will be treated as plain text
- This is a security boundary - ops cannot execute via messaging surfaces
```

## Why This Matters

If agents could trigger file operations by sending `ops: [{"kind":"fs_write"...}]` to Telegram, any compromised account could execute arbitrary file operations on the server.

**Escalating:** I cannot complete this MCA due to file access issues. This needs human intervention to:
1. Add the security boundary section to agent-ops documentation
2. Ensure all agents understand this critical restriction

surface: BLOCKED: Cannot update agent-ops docs with ops: security boundary. Need human to add rule that ops: only works in output.md, never in messaging.