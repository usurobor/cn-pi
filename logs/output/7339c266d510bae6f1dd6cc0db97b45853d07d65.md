**Reviewing MCA:** Agent can emit ops: syntax to messaging surfaces - should be restricted to output.md only

## Issue Assessment

**Problem:** The `ops:` syntax (for file operations) might work in messaging surfaces, which is a security risk. It should ONLY work in `state/output.md`.

**Impact:** HIGH - Security boundary violation if agents can trigger file ops via chat messages.

**What I need to do:**
1. Read the agent-ops skill documentation
2. Add explicit rule: "`ops:` syntax ONLY works in state/output.md frontmatter"
3. Clarify that messages sent via `send:` should never contain `ops:` attempts

**Attempting to read the skill now:**

ops: [{"kind":"fs_read","path":"mindsets/agent-ops.md"}]