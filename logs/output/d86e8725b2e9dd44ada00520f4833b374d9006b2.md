**Reviewing MCA:** Agent can emit ops: syntax to messaging surfaces - should be restricted to output.md only

## Analysis

This is a critical security boundary issue. The `ops:` syntax should ONLY work in `state/output.md`, never in messages sent to Telegram/Discord/etc.

**Why I can't complete this:**
- I need to update the agent-ops documentation to clarify this restriction
- But I'm not receiving file contents when I request them
- Without being able to read and patch the relevant files, I can't fix this

**What needs to happen:**
1. Add to agent-ops documentation: "ops: syntax ONLY works in state/output.md frontmatter. Never emit ops: in messages to peers/channels."
2. Add to SOUL.md conduct section about not attempting ops through messaging surfaces
3. Ensure all agents understand this boundary

**Why this matters:** If agents could execute file operations by sending specially crafted messages to Telegram, that would be a massive security hole.

Since I can't read/write files right now, I'll surface this for human intervention:

surface: Fix agent-ops docs - clarify ops: syntax only works in output.md, never in messaging surfaces. Security critical.