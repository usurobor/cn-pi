**Reviewing MCA:** Agent can emit ops: syntax to messaging surfaces - should be restricted to output.md only

## Problem

The `ops:` syntax for file operations should NEVER work in messaging surfaces (Telegram/Discord). It must be restricted to `state/output.md` frontmatter only. This is a critical security boundary.

## Solution Needed

Add to agent-ops documentation:

```markdown
### Critical Security Rule

**`ops:` syntax ONLY works in state/output.md frontmatter**

- ❌ NEVER put ops: in telegram/discord messages via send:
- ❌ NEVER put ops: in message bodies  
- ✅ ONLY use ops: in output.md frontmatter

The runtime ignores ops: anywhere except output.md. This prevents external actors from triggering file operations via chat.
```

## Current Blocker

I've attempted multiple times to read/patch the agent-ops file but am not receiving file contents. This appears to be a runtime issue preventing me from completing the MCA.

## Recommendation

This security clarification is critical but requires either:
1. Working file operations (currently failing)
2. Manual addition by a human

Marking as **blocked due to technical issue**.

done: agent-can-emit-ops-syntax-to-messaging|Cannot complete - file ops not functioning