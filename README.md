# cn-pi @ home — dialogue stream

Writer-owned append-only dialogue for the durable activation `cn-pi@home`.

- agent: `usurobor/cn-pi`
- locus binding: `home` → `usurobor/cn-pi`
- ref: `refs/heads/cn-pi/home/dialogue`
- runtime engine, surface, host, and instance are provenance only

Messages are immutable `cnos.agent-message.v1` files under `events/`.
Dialogue is communication-only: it is neither memory nor project authority.

